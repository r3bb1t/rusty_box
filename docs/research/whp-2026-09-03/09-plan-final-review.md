# Final review — `docs/superpowers/plans/2026-09-03-whp-vmm-shape.md`, Stage 1 (Tasks 1.1–1.9)

Read-only check of every tree fact the plan asserts, plus internal name consistency, at head `7728086`.

## Part 1 — WHP leaf, engine crate, core, machine mod.rs

- [wrong] Task 1.3, "`pic_pin_published: bool` … reset to `false` by the machine's reset paths at mod.rs:956/1029" — mod.rs:956 is inside `Emulator::new_from_parts` (mod.rs:909) and mod.rs:1029 is inside `Emulator::init_at` (mod.rs:974); both are placement-**construction** sites (`addr_of_mut!((*ptr).stop_cause).write(..)`), not reset paths. The real reset path is `Emulator::reset` (mod.rs:1370, and `run.rs:93`), which the plan never names — leaving `pic_pin_published == true` across a guest reset would swallow the next rising edge. — fix: say "initialised at mod.rs:956/1029 (the two placement constructors) **and cleared in `Emulator::reset`, mod.rs:1370**".
- [wrong] Task 1.1, "`Partition` keeps: map/…/set_property_late … the other per-VP verbs on `Partition` are deleted (breaking; there are no external users)" — six per-VP verbs exist that are neither in the move list nor in the keep list and **do** have users: `read_reg` (partition.rs:659), `write_reg` (735), `read_segments` (760), `write_segments` (743), `read_tables` (773), `write_tables` (786). Live callers: `rusty_box_whp/examples/whp_probe.rs:167` (`self.partition.write_segments`), `rusty_box_whp_engine/examples/step_bench.rs:189/193/194/203/204` (`partition.read_reg`/`write_reg`), plus partition.rs's own internals at 801/822/836 and its tests at 1029/1066/1073/1340. The `Vcpu` Produces block declares none of them. (`state.rs:505/510/522/523` are *not* affected — those are `Recorder`'s own inherent methods.) — fix: list `read_reg`/`write_reg`/`read_segments`/`write_segments`/`read_tables`/`write_tables` on `Vcpu` too, or say explicitly which of them die and what `whp_probe`/`step_bench` use instead.
- [minor] Task 1.1, "DELETE the `Vp<'_>` adapter at lib.rs:80-110" — the adapter is lib.rs:80–107 (`impl state::VpRegisters for Vp<'_>` closes at 107); 108 is blank, 109 `#[cfg(test)]`, 110 `mod tests {`. — fix: 80–107.
- [minor] Task 1.4, "`InjectState::at_reset()` (engine.rs:376…)" — it is `const fn at_reset()` at engine.rs:375. — fix: 375.
- [minor] Task 1.4, "`VpContext { … }` (vcpu.rs:414-431, all fields `pub`)" — the struct is vcpu.rs:414–425 (fields 415–424, all `pub` ✓); 427–441 is `Exit`/`impl Exit`. — fix: 414–425.

Confirmed correct in this part (no action): `Partition::run` 634, `cancel_run` 642, `read_regs` 651, `write_regs` 669, `read_xsave` 683, `write_xsave` 692, `read_registers` 708, `write_registers` 722, `inject` 800, `internal_activity` 820, `set_internal_activity` 831, `intercept_counters` 852, `runtime_counters` 866, `translate_gva` 880, `mapped_regions` 552, `set_property_late` 895; `Canceller` 906 / `InterruptRequester` 936 / `request` 946; the lifetime doc block at 556–569; `LocalApicMode` 39; `halting_partition` 1020, `a_turn_on_the_hardware` 1041, `run_until_halt_with(partition: &mut Partition, code: &[u8])` 1062, `the_platform_counts_…_halts` 1204. Task 0.2 does declare `reference_time_100ns`/`suspend_time`/`resume_time`/`read_apic_state`/`write_apic_state`/`read_words128`/`write_words128`, so 1.1's move list is self-consistent for those.
`SegmentRegister { base: u64, limit: u32, selector: u16, attributes: u16 }` vcpu.rs:114–122; `Reg` vcpu.rs:14; every `ExitReason` variant the 1.5/1.6 service match names exists (vcpu.rs:385–410), and `ApicWriteTrap` is indeed payload-less today.
engine.rs: `WHP_ALL_SHADOW` 209, `WHP_MIN_SLICE_US` 2914, `ExitCounts` 578 (`#[non_exhaustive]`; existing fields port/memory/cpuid/msr/halt/canceled/boundary/window), `InjectState` 344, `refresh_from` 385, `Started` 475 with the field-order comment at **470–474** and all six named fields (`alarm` 476, `shadowed` 512, `held_interrupt_state` 523, `held` 534, `ran_since_read_back` 548, `consecutive_mmio` 555), `service_port_access` 2780, the **ExitHistory classification match at 1986–2003** and the **service match at 2005, closing at 2143** — both exactly as stated. Every symbol in 1.7's DELETE and KEEP lists exists (`run_the_exit_loop` 1844, `run_slice_on_the_shadow` 1464, `run_until_the_machine_is_needed` 1818, `install_the_shadow` 1549, `impose_the_shadow` 1657, `read_back_into_the_shadow` 1703, `impose_after_errand` 2710, `burst_on_the_shadow` 2663, `everything_on_the_shadow` 207, `shortest_hardware_slice` 2911, `SLICE_RESOLUTION` 2889, `HARDWARE_SPEED` 2942 / `hardware_speed` 2966 / `default_hardware_speed` 2961, `host_time_exactly` 2865 / `host_time_for` 2854 / `deadline` 2844 / `ticks_elapsed` 2982, `Yielded` 953, `BURST_AFTER` 2638, `SliceCensus` 613; kept: `install_the_machines_map` 1023, `memory_map_changed` 1115, `TRAPPED_MSRS` 114, `TRAPPED_CPUID_LEAVES` 70, `InjectCensus` 716, `PlatformCounters` 838, `report_the_state_the_platform_refused` 2414, `ExitHistory` 128; also `report_the_fault` 2515, `finish_on_the_shadow` 2686, `run_the_shadow_out_of_smm` 2376, `stage_injection` 2267).
state.rs: `pub(crate) trait VpRegisters` at **138** with exactly `read_words/write_words/read_registers/write_registers`; `Recorder` at **427** inside the private `#[cfg(test)] mod tests` at **418**. xsave.rs: `of_this_host` at **100/116**, `refresh_from(&Partition, u32)` 278 / `write_to(&Partition, u32)` 287, `fill` 298, `patch` 412.
engine lib.rs: `machine_running` 129 (`with_engine`, no devices), `machine_with_devices` 150 (`MachineBuilder::build_on::<WhpEngine>`), `hypervisor_here` 195, `a_turn_on_the_hardware` 215, `trace_of` 171, `a_device_interrupt_reaches_a_hardware_guest_by_injection` 631, and all four injection tests 1314/1469/1602/1733 plus the two slice tests 469/512 named for removal.
rusty_box_core `engine.rs`: `EngineFault` struct with private fields and `new(kind, at)` 334 / `with_code(kind, at, code)` 340 / `code()` 357 / `at()` 363; `EngineFaultKind` `#[non_exhaustive]` 302–303 with `Unsupported/Memory/Vcpu/State/Host` (every kind the plan uses exists).
mod.rs: `Emulator::ticks()` = `pc_system.time_ticks()` at 1559/1560; `run_slice`'s six-field destructuring at **634–652**; `device_manager` `pub(crate)` 486, `pc_system` `pub(crate)` 488, `config` **private** 503 (as the plan states); the `assert_send::<Emulator<()>>()` block at **2188–2191**. run.rs: `Emulator::engine()` is `pub` at **911**; `RunBudget::{Instructions, Ticks}` 271–283.
`BatchOutcome` — the plan never mentions it; nothing to check.

## Part 2 — machine crate, arithmetic, and internal consistency

- [wrong] Task 1.4, "`next_instruction_touches_vector_state` … (the decoder's `is_sse_or_avx()` class bit)" — `is_sse_or_avx` **does not exist anywhere in the tree** (full grep over all `*.rs`, no truncation: zero hits; the nearest things in `rusty_box/src/cpu/decoder/mod.rs` are `is_avx512_subfeature` at 152 and the ISA-extension machinery behind `BX_ISA_EXTENSIONS_ARRAY_SIZE`, `rusty_box_decoder/src/lib.rs:42`). — fix: either name the real source (the per-opcode ISA-extension requirement the icache ISA gate already computes) or move the class bit into the Produces block as new work.
- [wrong] Task 1.6, `EngineFault::with_code(EngineFaultKind::Vcpu, "WHvRequestInterrupt", e.code())` and the same in `pic_pin_changed` — `WhpError` has **no** `code()`; its accessors are `hresult() -> i32` (error.rs:70), `kind()` (63) and `call()` (76). (`EngineFault::code()` is a different type's method.) — fix: `e.hresult()` in both places.
- [wrong] Task 1.7, the `service_once` unit test builds `MachineBuilder::new(EmulatorConfig::default()).build()` ("an interpreter machine") and calls `service_once(&mut machine, &clock)`, but `service_once` is declared `pub(crate) fn service_once<T: Instrumentation, H: HostClock>(machine: &mut Emulator<T, WhpEngine>, …)` — `build()` returns `Box<Emulator<T, SoftwareEngine>>` (builder.rs:437, `build_on::<SoftwareEngine>()`), so the test cannot compile against that signature. — fix: make `service_once` generic in the engine (`E: SliceEngine<T>`), or build the test machine with `build_on::<WhpEngine>()`.
- [wrong] Task 1.7, `a_guest_that_halts_stays_parked_in_the_hypervisor_and_wakes_for_a_device_interrupt` reuses "the PIT guest of `a_device_interrupt_reaches_a_hardware_guest_by_injection`" — that guest (lib.rs:640–678) never halts inside the measured window: it is `mov cx,0x4000` / `cli` / poll IRR / `sti` / `nop` / `loop trial`, reaching its `hlt` (CODE+0x32) only after 16 384 PIT ticks ≈ 14.06 s of VM time, while the test runs 40 × 10 ms = 400 ms (~466 ticks). The named property (halted guest parked in the hypervisor, woken by the device thread) is never exercised, and nothing asserts `census.exits.halt`. — fix: use a guest whose loop is `sti; hlt; jmp $` (or add the halt assertion and a guest that halts), and say what `exits.halt` must be.
- [minor] Task 1.5, `EngineFaultKind::Wedged` is added to `rusty_box_core/src/engine.rs` but **no task ever constructs it** — every wedge path in the plan uses `FastMachineFault::Wedged { waited }` (1.7) instead (plan lines 18, 1256, 1278, 1292, 1396). — fix: drop `Wedged` from 1.5, or say which path builds it.
- [minor] Task 1.7, "`device_thread.rs` unit tests (hypervisor-free, `SharedClock` **from the fixtures**)" — `SharedClock` is declared only as a local `struct` inside Task 1.2's `vm_clock.rs` test module (plan line 595); the `fixtures` block (Task 1.4, engine `lib.rs`) never lists it, and no task moves it. A `#[cfg(test)] mod tests` item in `vm_clock.rs` is not reachable from `device_thread.rs`'s tests. — fix: declare `SharedClock` in `fixtures` in 1.2 (or have 1.7 hoist it there explicitly).
- [minor] Task 1.4, the Produces block puts `import_arch_groups`/`export_arch_groups` in `rusty_box/src/cpu/api_bridge.rs`, but their whole-state counterparts `import_arch_state` (arch_state.rs:751) and `export_arch_state` (arch_state.rs:665) live in `arch_state.rs`, which Step 3 says they generalise ("the whole-state pair becomes the `all()` case"). Only `load_segment` (api_bridge.rs:206) is where the plan says. — fix: put the two group functions in `arch_state.rs` beside the pair they filter.
- [minor] Task 1.3, `furnished_machine_on(MapWatchingEngine::default())` is described as "`furnished_machine` over `Emulator::<(), E>::with_engine`", but `with_engine(config: EmulatorConfig, mode: CpuSetupMode)` (emulator_api.rs:960, on `impl<E: SliceEngine<()> + Default> Emulator<(), E>`) takes **no engine value** — it default-constructs one, so the argument is dropped unused. — fix: `furnished_machine_on::<E>()` with no argument, or a constructor that accepts an engine value.
- [minor] Task 1.3, the comment `// 7_500 ticks at Ips::BOCHS_DEFAULT` in `service_device_time_fires_every_due_period_and_names_the_next_deadline` — `furnished_machine` is factored from `pit_irq0_delivers_through_ioapic_pin2_to_cpu0`, whose config is `Ips::new(1_000_000)` (tests.rs:881–884), not `Ips::BOCHS_DEFAULT`; at 1 MHz the 8042 period is 150 ticks. The test's arithmetic is ips-derived so it still holds, but the annotation contradicts the source it is factored from. — fix: state which `ips` `furnished_machine` uses.
- [minor] Task 1.4 Step 5, the segment-validation tests are written over raw attribute words ("`ds.attributes == present-only`", "`attributes 0x93, limit 0xFFFF`") — `SegmentState.attributes` is `SegmentAttributes` (arch_state.rs:92), a newtype over `u16` built with `SegmentAttributes::from_bits(..)`, not a bare word. — fix: spell the constructor in the test sketch.
- [minor] Tasks 1.3 and 1.6 both declare a helper named `program_ioapic_entry` with **different shapes**: 1.3's is `(machine, pin, vector)` in `emulator/tests.rs`; 1.6's is `(fabric, pin, vector, level: bool)` in `iodev/irq.rs`. Both are `rusty_box` test helpers. — fix: give one of them a distinct name, or unify on the 4-arg form.
- [minor] Task 1.5 declares `ExitReason::name(&self) -> &'static str` as "a new `const fn`", but `ExitReason` lives in `rusty_box_whp/src/vcpu.rs:385` and that file is **not** in Task 1.5's Files list (which names only engine.rs, engine lib.rs, engine Cargo.toml and `rusty_box_core/src/engine.rs`). — fix: add `rusty_box_whp/src/vcpu.rs` to 1.5's Modify list.
- [minor] Task 1.6 Files, "`rusty_box/src/pc_system.rs` (`device_clock: DeviceClock` field set at `initialize`…)" — `BxPcSystemC::initialize(&mut self, ips: u32)` (pc_system.rs:635) takes only `ips`, so "set at `initialize`" means a signature change with existing callers (e.g. `emulator/tests.rs:892`), which the Files list does not mention; the same bullet then says `init_memory_and_pc_system` copies it, which is a different mechanism. — fix: pick one (a setter called from `init_memory_and_pc_system` is the non-breaking one) and say so.

### Arithmetic (all confirmed)
`Ips::BOCHS_DEFAULT = Self(50_000_000)` (mod.rs:155) and `EmulatorConfig::default()` uses it (mod.rs:296). `KBD_SERIAL_DELAY_USEC = 150` (keyboard.rs:271) → 50 000 000 × 150 / 1 000 000 = **7 500 ticks** ✓. 400 µs at 50 MHz = **20 000 ticks** ✓ (and `next_deadline` after 20 000 with a 7 500 period is 22 500, inside the asserted `(20 000, 27 500]` ✓). 10 ms = ips/100 = **500 000 ticks** ✓; `ips / 500` = 100 000 ticks = 2 ms, matching `alarm.rs`'s `SPIN_MARGIN = Duration::from_millis(2)` (alarm.rs:58) ✓. IOAPIC-edge guest: PIT count 0x1000 = 4096 at 1.193182 MHz = 4096/1 193 182 = **3.433 ms** ✓ ("~3.4 ms"). Halt-wake guest: count 0x400 = 1024 → **858.2 µs** ✓, and 400 ms / 858.2 µs = **466 ticks** ✓ ("~466"); the reused guest at lib.rs:653–656 does program count 0x0400 ✓.

### Also confirmed (no action)
scheduler.rs: `service_scheduler_boundary(elapsed_ticks: u64) -> CpuResult<bool>` is `pub` at **972**, its `return Ok(true)` (reset applied, `elapsed_ticks` not ticked) at **1029**, `Ok(false)` at **1175**, and the two `stop_cause = StopCause::GuestPowerOff` + `stop_flag` sites at **985/997** — every number exact; `deliver_ioapic_to_lapics` **714–773**; `deliver_lapic_bus_interrupt` **574–597**; the `tickn(step as u32)` deadline-sized loop at **1166** (within 1142–1166); `sync_final_event_levels` at **1220**, publishing `int_pin_asserted()` as a LEVEL into `BX_EVENT_PENDING_INTR` and calling `complete_deferred_delivery(delivery, done)`. pc_system.rs: `next_timer_deadline_at() -> Option<u64>` 1316; the continuous-timer catch-up `while time_to_fire <= ticks_total` at **744–746** (the coalescing the plan warns about). ioapic.rs: `PendingIoApicDelivery` at **307** with all seven named fields; `set_pin_level`'s pin 0 → 2 remap at **698–702**; `BxIoApic::service` **786**; `take_pending_deliveries` returns `(array, count)` **883**; `complete_deferred_delivery` 894; `trigger_mode()` 202. irq.rs: `IrqFabric` pub at 61, `new` 77, `service_ioapic` **179**, `raise` 129, `set_ioapic_pin` 148, `mmio_write` 170, `receive_eoi` 231, `int_pin_asserted` 203, `acknowledge` 212 (all `pub(crate)`, reachable from the in-crate tests the plan writes); `rusty_box::iodev` and `iodev::irq` are both `pub`, so a new `pub struct IoApicDelivery` there is nameable from the engine crate.
keyboard.rs: `KBD_SERIAL_DELAY_USEC: u32 = 150` at **271**, `timer_fired(.., fires: u32, ..)` at **2724**. timers.rs: `rearm_device_timers_after_hardware_reset` **177**, `fire_keyboard_timer` **482**, `register_timer_owners` 21. iodev/mod.rs: `TimerRequest::Activate { deadline_ticks, period_ticks, continuous }` at **189**(–198); `scheduler_boundary_requested` latched at **620** and **823**; `DeviceTimerOwner::Keyboard` 155. emulator/tests.rs: `MapWatchingEngine` **36**, `pit_irq0_delivers_through_ioapic_pin2_to_cpu0` **872** (setup lines 878–894). `IrqLine(pub u8)` rusty_box_devices/src/api.rs:20.
rusty_box_core/time.rs: `ClockHz::new(hz) -> Option<Self>` 55, `ticks_from_nanos_floor -> VmDuration` 154, `nanos_at(VmInstant) -> u64` 89, `VmInstant::{from_ticks 176, ticks 181, since 191, add 198}`, `VmDuration::from_ticks` 217, `HostInstant::{from_nanos 322, as_nanos 327, nanos_since 333}`, `trait HostClock { fn now(&self) -> HostInstant }` 345, `ManualClock` `Copy` with `advance_nanos(&mut self)` 351–368 — the plan's justification for a separate `SharedClock` is correct.
arch_state.rs: `VcpuArchState` at **324**, `#[non_exhaustive]`, with every field the plan lists (cr0/cr2/cr3/cr4 315+, cr8, dr[4], dr6, dr7, xcr0 included) and a hand-written `Default` at 360; `import_arch_state(&mut self, &VcpuArchState) -> Result<(), ArchStateError>` at **751**; `ArchStateError` `#[non_exhaustive]` 395; `set_lapic_tpr_from_cr8` 598. api_bridge.rs `load_segment` at **206**.
memory/plan.rs: `LOCAL_APIC_REGION = 0xFEE0_0000..0xFEF0_0000` at **45**, `carve_outs.add(..)` at **115**; `BX_PCI_HOLE_START = 0xC000_0000`. xtask/src/ci.rs: "WHP leaf tests" = `cargo test --release -p rusty_box_whp` with **no `--lib`** at **521–522** (so the `compile_fail` doctest of 1.1 does run); the unsafe-token baselines `("rusty_box_whp/src", 38)` at **111** and `("rusty_box_whp_engine/src", 1)` at **119**; root `Cargo.toml` `[profile.release] panic = "abort"` at **37–38**; `rusty_box_whp_engine/Cargo.toml` has **no** `[lints]` section, and the crate's one lift is `#![expect(unsafe_code, reason = …)]` at lib.rs:44–47 discharged in `map_window` (engine.rs:1084).
Other: `alarm.rs` `SPIN_MARGIN = 2 ms` at 58 (the two-phase wait the plan moves); `PcIo::{memory 65, devices 71, device_manager 77, pc_system 83}`; `PcIo::{emulate_one 148, pop_deliverable_vector 168, emulate_batch 195, finish_the_instruction 244}`; `acknowledge_external_interrupt` at event.rs:**661**; `PlatformCounters { intercepts, runtime }` is `Copy` and `RuntimeCounters { total_100ns, hypervisor_100ns }`, so `VcpuCensus: Copy` and `census.vcpus[0]` work; `InjectCensus.injected`; `MsrExits.apic_base_write` (caps.rs:49) and `ExtendedVmExits` (caps.rs:115) exist; `LocalApicMode` / `PartitionConfig::local_apic` 260; `pc_system.initialize(&mut self, ips: u32)` 635; `init_memory_and_pc_system` mod.rs:1067; `whp_probe.rs` `struct Guest` 139; `alpine_probe.rs` milestone site **225–232** and `census.slices` at 252; `CpuError::UnsupportedCpuOperation { operation: &'static str }` (error.rs:49) matches `EngineFault::at() -> &'static str`; `pub trait Instrumentation: Default + 'static` at instrumentation/bochs.rs:**84**; `assert_send::<Partition>()` and `assert_send_sync` both already in rusty_box_whp/src/lib.rs:68–76; `ExitCounts::total()` does not exist today and is correctly listed as new. `lapic.write_aligned(0xF0, …)` is valid despite the `BxPhyAddress` parameter — `decode_register` masks `addr & 0xFF0` (apic.rs:838), so a bare offset decodes to SVR; `lapic.acknowledge_int() -> u8` 1626; `pending_event: u32` cpu.rs:579 and `BX_EVENT_PENDING_LAPIC_INTR` cpu.rs:1116; `BxPicC.master: Pic8259State` (pic.rs:384) with `imr: u8` (209). No name collisions in the tree for `Processor`, `DeviceClock`, `DeviceTime`, `StdClock`, `EngineCensus`, `StepOutcome`, `StepStop`, `Vcpu`, `Exchange`, `ArchGroups`, `service_device_time`.
Internal consistency otherwise holds: `Vcpu`/`VpCounters`/`take_vcpu` (1.1) → 1.5/1.7; `VmClockSource`/`StdClock` (1.2) → 1.7; `Processor`/`DeviceTime`/`DeviceClock`/`service_device_time`/`DeliveryRoute`/`IoApicDelivery` (1.3) → 1.5–1.7; `ArchGroups`/`Exchange`/`ExitClass` and `import_for`'s five-argument shape (1.4) → its own tests and 1.5; `VcpuControl::{request_park, raise_ext_int, wait_parked_by, resume, stop, census}`, `Parked`, `VcpuCensus`, `bring_up`, `install_control` (1.5) → 1.6/1.7; `FastMachine::{adopt, step, pause, resume, with_machine, engine_census}`, `StepOutcome`, `StepStop`, `FastMachineFault`, `EngineCensus`, `DeviceThreadControl`, `service_once` (1.7) → 1.8; `fixtures::{shared, wait_until, machine_with_devices_on}` all declared before use. Task 1.9's `Send`-assertion list is fully covered by 1.1 (`Vcpu`, `VpCounters`), 1.5 (`Emulator<(), WhpEngine>`, `VcpuControl`, `VcpuThread<()>`) and 1.7 (`DeviceThreadControl`, `FastMachine<()>`).

Confirmed: ~245 claims checked, 6 wrong, 11 minor
